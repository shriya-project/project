# Stroke Risk Prediction Project

This folder mirrors the root implementation. For latest workflow and documentation, use:

- training entrypoint: `python -m src.train`
- app entrypoint: `streamlit run app/streamlit_app.py`
- full docs: root `README.md`
